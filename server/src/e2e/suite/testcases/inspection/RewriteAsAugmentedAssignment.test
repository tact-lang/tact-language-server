========================================================================
Assign with simple case
========================================================================
fun foo() {
    let a = 100;
    a = a + 10;
}
------------------------------------------------------------------------
2 2:4 to 2:14 Can be rewritten as `a += 10` (tact)

========================================================================
Assign with simple case 2
========================================================================
fun foo() {
    let a = 100;
    a = b + 10;
}
------------------------------------------------------------------------
no issues

========================================================================
Assign with simple reverse case
========================================================================
fun foo() {
    let a = 100;
    a = 10 + a;
}
------------------------------------------------------------------------
2 2:4 to 2:14 Can be rewritten as `a += 10` (tact)

========================================================================
Assign with parens
========================================================================
fun foo() {
    let a = 100;
    a = (a + 10);
}
------------------------------------------------------------------------
2 2:4 to 2:16 Can be rewritten as `a += 10` (tact)

========================================================================
Assign with parens 2
========================================================================
fun foo() {
    let a = 100;
    a = a * (10 + 20);
}
------------------------------------------------------------------------
2 2:4 to 2:21 Can be rewritten as `a *= 10 + 20` (tact)

========================================================================
Assign with field access
========================================================================
fun foo() {
    a.bar = a.bar + 10;
}
------------------------------------------------------------------------
2 1:4 to 1:22 Can be rewritten as `a.bar += 10` (tact)

========================================================================
Assign with field access 2
========================================================================
fun foo() {
    a.bar = a.baz + 10;
}
------------------------------------------------------------------------
no issues

========================================================================
Assign with field access reversed
========================================================================
fun foo() {
    a.bar = 10 + a.bar;
}
------------------------------------------------------------------------
2 1:4 to 1:22 Can be rewritten as `a.bar += 10` (tact)

========================================================================
Assign with all supported operators
========================================================================
fun foo() {
    let a = 100;

    a = a + 10;
    a = 10 + a;
    a = a - 10;
    a = 10 - a;
    a = a * 10;
    a = 10 * a;
    a = a / 10;
    a = 10 / a;
    a = a % 10;
    a = 10 % a;
    a = a & 10;
    a = 10 & a;
    a = a | 10;
    a = 10 | a;
    a = a && 10;
    a = 10 && a;
    a = a || 10;
    a = 10 || a;
    a = a << 10;
    a = 10 << a;
    a = a >> 10;
    a = 10 >> a;
    a = a ^ 10;
    a = 10 ^ a;
}
------------------------------------------------------------------------
2 3:4 to 3:14 Can be rewritten as `a += 10` (tact)
2 4:4 to 4:14 Can be rewritten as `a += 10` (tact)
2 5:4 to 5:14 Can be rewritten as `a -= 10` (tact)
2 7:4 to 7:14 Can be rewritten as `a *= 10` (tact)
2 8:4 to 8:14 Can be rewritten as `a *= 10` (tact)
2 9:4 to 9:14 Can be rewritten as `a /= 10` (tact)
2 11:4 to 11:14 Can be rewritten as `a %= 10` (tact)
2 13:4 to 13:14 Can be rewritten as `a &= 10` (tact)
2 14:4 to 14:14 Can be rewritten as `a &= 10` (tact)
2 15:4 to 15:14 Can be rewritten as `a |= 10` (tact)
2 16:4 to 16:14 Can be rewritten as `a |= 10` (tact)
2 17:4 to 17:15 Can be rewritten as `a &&= 10` (tact)
2 18:4 to 18:15 Can be rewritten as `a &&= 10` (tact)
2 19:4 to 19:15 Can be rewritten as `a ||= 10` (tact)
2 20:4 to 20:15 Can be rewritten as `a ||= 10` (tact)
2 21:4 to 21:15 Can be rewritten as `a <<= 10` (tact)
2 23:4 to 23:15 Can be rewritten as `a >>= 10` (tact)
2 25:4 to 25:14 Can be rewritten as `a ^= 10` (tact)
2 26:4 to 26:14 Can be rewritten as `a ^= 10` (tact)
