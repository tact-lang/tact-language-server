========================================================================
Log function with 2
========================================================================
fun foo() {
    let _ = log(10, 2);
}
------------------------------------------------------------------------
2 1:12 to 1:22 Function `log` can be rewritten as more efficient `log2` (tact)

========================================================================
Log function with 3
========================================================================
fun foo() {
    let _ = log(10, 3);
}
------------------------------------------------------------------------
no issues

========================================================================
Pow function with 2
========================================================================
fun foo() {
    let _ = pow(10, 2);
}
------------------------------------------------------------------------
2 1:12 to 1:22 Function `pow` can be rewritten as more efficient `pow2` (tact)

========================================================================
Pow function with 3
========================================================================
fun foo() {
    let _ = pow(10, 3);
}
------------------------------------------------------------------------
no issues
